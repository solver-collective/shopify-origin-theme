{% comment %}

  DESCRIPTION:
  The index.liquid template renders the home page of the store. The template is loaded when you go to the root ("/") of the store.

  DOCUMENTATION:
  https://help.shopify.com/en/themes/development/templates/index-liquid

  ---

  REQUIREMENTS:
  In order to output dynamic sections built in the theme editor, the index.liquid template must include the {{ content_for_index }} object.

{% endcomment %}